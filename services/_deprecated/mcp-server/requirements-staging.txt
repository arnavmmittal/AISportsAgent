# Staging Requirements - ML Features without Heavy Embedding Models
# Target: <1GB memory (vs 8-9GB for full deployment)
# Enables: XGBoost predictions, correlation analysis, slump detection
# Disables: Local embedding models (uses OpenAI API instead)

# ============================================
# Core Framework (~100MB)
# ============================================
fastapi==0.115.0
uvicorn[standard]==0.32.0
gunicorn==21.2.0
pydantic==2.10.0
pydantic-settings==2.6.0

# ============================================
# Database (~30MB)
# ============================================
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# ============================================
# AI/ML - OpenAI Only (~50MB)
# No PyTorch! Uses API for embeddings
# ============================================
openai==1.58.1
tiktoken==0.7.0

# ============================================
# ML/Statistics for Predictions (~400MB)
# XGBoost, scikit-learn, scipy
# ============================================
scikit-learn==1.5.2
scipy==1.14.1
xgboost==2.1.3
shap==0.46.0

# ============================================
# Auth (~20MB)
# ============================================
python-jose[cryptography]==3.3.0
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
python-multipart==0.0.20

# ============================================
# Utilities (~30MB)
# ============================================
python-dotenv==1.0.1
httpx==0.28.1
numpy==1.26.4
aiohttp==3.9.5
redis==5.0.1

# ============================================
# PDF Processing (~10MB)
# ============================================
pypdf==5.1.0

# ============================================
# Monitoring
# ============================================
loguru==0.7.3

# ============================================
# NOT INCLUDED (saves ~8GB):
# - sentence-transformers (pulls PyTorch ~2GB + models ~500MB)
# - chromadb (uses sentence-transformers internally)
# - torch, torchvision
# - langchain (full) - only langchain-openai if needed
# ============================================

# Estimated total: ~640MB (well under 1GB limit)
